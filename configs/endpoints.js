const SERVICE_URL = 'https://developers.paysera.com/tasks/api';

const CASH_IN_FEES = `${SERVICE_URL}/cash-in`;
const CASH_OUT_JURIDICAL = `${SERVICE_URL}/cash-out-juridical`;
const CASH_OUT_NATURAL = `${SERVICE_URL}/cash-out-natural`;

module.exports = {
  CASH_IN_FEES,
  CASH_OUT_JURIDICAL,
  CASH_OUT_NATURAL,
}
